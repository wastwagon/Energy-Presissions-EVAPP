/** Minimum wallet balance (GHS) required to start a metered charging session. */
export const MIN_WALLET_START_BALANCE = 5;

/** Stop metered session when balance cannot cover this many kWh at current tariff. */
export const METERED_BALANCE_STOP_KWH_BUFFER = 0.05;
