/**
 * Canonical logo in `public/` — transparent PNG for use on any background.
 * Replace `public/logo.png` with your export from design tools (keep filename or update here).
 */
export const LOGO_PUBLIC_URL = '/logo.png';
